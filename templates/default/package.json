{
  "name": "__APP_NAME__",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy",
    "check": "node --check worker.js && find site public/app public/nativefragments -name '*.js' -print0 | xargs -0 -n1 node --check"
  },
  "dependencies": {
    "@nativefragments/core": "^0.2.0",
    "@nativefragments/signals": "^0.1.0",
    "hono": "^4.12.23"
  }
}
