# nativefragments.org

The public Native Fragments docsite.

This is intentionally separate from the core framework package. The core repo
ships only runtime code, API reference docs, and the agent skill.

```sh
npm install
npm run dev
```

Deploy:

```sh
npm run deploy
```
