{
  "name": "docs.nativefragments.org",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:api": "node scripts/generate-api-reference.js",
    "dev": "npm run generate:api && npx wrangler dev",
    "deploy": "npm run generate:api && npx wrangler deploy",
    "check": "npm run generate:api && node --check worker.js && find site public/app public/nativefragments scripts -name '*.js' -print0 | xargs -0 -n1 node --check"
  },
  "dependencies": {
    "@nativefragments/core": "^0.1.2"
  }
}
