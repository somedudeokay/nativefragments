---
"@nativefragments/core": minor
---

Add first-class fragment prefetching with shared in-flight request deduplication, exported `prefetchFragment()`, default intent prefetch, and declarative `data-fragment-prefetch` modes.
